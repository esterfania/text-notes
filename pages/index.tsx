import Head from "next/head";
import Layout, { siteTitle } from "../components/layout";
import utilStyles from "../styles/utils.module.css";
import { getSortedPostsData } from "../lib/posts";
import Link from "next/link";
import Date from "../components/date";

export default function Home({ allPostsData }) {
  return (
    <Layout home>
      <Head>
        <title>{siteTitle}</title>
      </Head>
      <section className={utilStyles.headingMd}>
        <p>
          Desenvolvedora web, pós graduanda em ciência de dados, apaixonada por
          tecnologia. Na área de desenvolvimento de software participei de
          implementações de novas funcionalidades, correções de sistemas web,
          construção de páginas com foco na usabilidade, projetos de dashboards
          para análise de dados. Na maioria dos projetos que participei, atuei
          como desenvolvedora Full Stack. Atualmente estou focada em
          desenvolvimento Front-End. Tecnologias: JavaScript, TypeScript, CSS,
          HTML, AngularJS, Angular, C#, .Net, REST, TFS, GIT.{" "}
        </p>
      </section>
      <section className={`${utilStyles.headingMd} ${utilStyles.padding1px}`}>
        <h2 className={utilStyles.headingLg}>Blog</h2>
        <ul className={utilStyles.list}>
          {allPostsData.map(({ id, date, title }) => (
            <li className={utilStyles.listItem} key={id}>
              <Link href={`/posts/${id}`}>
                <a>{title}</a>
              </Link>
              <br />
              <small className={utilStyles.lightText}>
                <Date dateString={date} />
              </small>
            </li>
          ))}
        </ul>
      </section>
    </Layout>
  );
}

export async function getStaticProps() {
  const allPostsData = getSortedPostsData();
  return {
    props: {
      allPostsData,
    },
  };
}
